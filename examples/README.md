# examples/

Holds the "golden project" — a real service generated by Genesis, checked
in so CI can build it and prove generated code actually compiles and its
own generated tests pass (Epic 7, `docs/process/DEFINITION_OF_DONE.md`).

Empty until Epic 2/7 land.
